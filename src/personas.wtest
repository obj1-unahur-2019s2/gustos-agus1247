import personas.*
import objetos.*
import bolichito.*
// auto-generated at 29/08/2019 20:39:11
test "gustos de rosa" {
	
	assert.equals(true, rosa.leGusta(remera))
	assert.equals(false, rosa.leGusta(biblioteca))
	munieco.peso(1500)
	assert.equals(true, rosa.leGusta(munieco))
	
}

test "bolichito"
 
 assert.equals(true, bolichito.estaDesequilibrado())